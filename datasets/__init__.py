# Mark this directory as a regular package so it takes precedence over any
# unrelated ``datasets`` wheel (e.g. HuggingFace ``datasets``) installed in
# site-packages.  Flash3D's data loaders live here and are unrelated to HF.
